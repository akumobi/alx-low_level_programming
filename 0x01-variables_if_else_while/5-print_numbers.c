#include <stdio.h>
/**
 * main - program starts here
 * Return: program returns zero (0) when it runs successfully
 */
int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
		printf("%d", k);
	printf("\n");
	return (0);
}
